version: 2.0

dependencies:
    pre:
    - curl -fsSL https://git.io/v2Ifn | bash

jobs:
            
  build:
    docker: 
      - image: docker:17.06
    steps:
      - run: apk add --no-cache git openssh
      - checkout
      - run: echo hi
